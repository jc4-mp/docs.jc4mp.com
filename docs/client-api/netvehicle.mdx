import { InheritedComponent } from '../../src/components/InheritedComponent';

# NetVehicle

This is the client-side class for [NetVehicle](/shared-api/netvehicle). It has all the same methods and properties as the shared class, plus the following on this page.

Vehicle-related APIs. To use these, you must first have a reference to a NetVehicle, such as from a player's vehicle or from world vehicles.

## Class Instance Methods

<InheritedComponent className="NetVehicle_Client" />
